#include <iostream>
#include <cmath>
// or #include <bits/stdc++.h>
using namespace std;
const double pii(3.141593);
int illg()
{
	cout<<"I love Luogu!"<<endl;
	return 0;
}
int apple()
{
	int AUim(2+4),BWW(10-AUim);
	cout<<AUim<<" "<<BWW<<endl;
	return 0;
}
int aver()
{
	int all(14),mates(4),extra(0);
	cout<<(all/mates)<<"\n"<<((all/mates)*mates)<<"\n"<<(all%mates)<<endl;
	return 0;
}
int cola()
{
	double all(500),mates(3);
	cout<<(all/mates)<<endl;   //????
	return 0;
}
int train()
{
	int length1(260),v1(12),length2(220),v2(20),t(0);
	t=(length1+length2)/(v1+v2);
	cout<<t<<endl;
	return 0;
}
int duijiaoxian()
{
	cout<<(int)sqrt(pow(6,2)+pow(9,2))<<endl;
	return 0;
}
int Uim()
{
	int accou(100),op1(10),op2(20);
	cout<<accou+op1<<"\t"<<accou+op1-op2<<"\t";
	accou -= accou;
	cout<<accou<<endl;
	return 0;
}
int Circle()
{
	double r(5);
	cout<<pii*r*2<<"\n"<<pii*pow(r,2)<<"\n"<<(4/3)*pii*pow(r,3)<<endl;
	return 0;
}
int monkey()
{
	int init(1);
	for(int i(1);i<=3;i++)
		init=(init+1)*2;
	cout<<init<<endl;
	return 0;
}
int luogu()
{
	cout<<"9"<<endl; //???
	return 0;
}
int run()
{
	cout<<(100/3)<<endl;
	return 0;
}
int alphabet()
{
	char m('M'),a('A');
	cout<<(m-a+1)<<"\t"<<(a+17)<<endl;
	return 0;
}
int mud()
{
	int r1(4),r2(10),V(0);
	V = (4/3)*pii*pow(r1,3)+(4/3)*pii*pow(r2,3);
	cout<<pow(V,1/3)<<endl;
	return 0;
}
int gugu()
{
	int price(110),customer(10);
	//FC
}	
int main()
{
	return 0;
}